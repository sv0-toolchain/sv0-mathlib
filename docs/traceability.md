# Requirement traceability

Generated by `scripts/check_traceability.py --write-md` — do not hand-edit. Every
requirement in SPEC.md's tables (snapshot: [`requirements.tsv`](requirements.tsv), SPEC
commit `f6fc99b`) and the evidence that covers it:

- **test**: a `REQ: <ID>` tag in `test/` (the per-requirement matrix is
  [`requirement_test_matrix.md`](requirement_test_matrix.md));
- **check**: an automated CI check ([`traceability.tsv`](traceability.tsv));
- **process**: a manual process step, named there;
- **exception**: registered in [`exceptions.md`](exceptions.md);
- **future**: SPEC release `Future`.

| ID | Release | Evidence | Where | Requirement |
|---|---|---|---|---|
| CONV-001 | F0 | test, exception | test/unit/conv_review.md:12; MX-001 | The library SHALL expose one module per domain: math::arith, math::modular, math::trig, math::polar, ... |
| CONV-002 | F0 | test | test/unit/conv_review.md:22 | f64 SHALL be the primary floating-point type for every transcendental, polar, and complex function; every ... |
| CONV-003 | R0.2 | test | test/unit/conv_review.md:29 | An f32 variant SHALL be provided only where Section 15's error-bound table defines an explicit, separately ... |
| CONV-004 | F0 | test | test/unit/conv_review.md:39 | Every public function SHALL be a concrete, per-type function (e.g., sqrt_f64, gcd_u64), not a generic ... |
| CONV-005 | F0 | test | test/unit/conv_review.md:51 | A function with a mathematical domain restriction (division, sqrt of a negative real, asin/acos outside [-1, ... |
| CONV-006 | F0 | test | test/unit/conv_review.md:91 | The library SHALL NOT read %'s behavior on negative operands as a given. Every place floor/Euclidean ... |
| CONV-007 | F0 | test | test/unit/conv_review.md:102 | Every public function SHALL carry at least one requires clause (or an explicit "total function, no ... |
| CONV-008 | R0.1 | test | test/unit/conv_review.md:118 | Every function containing a while loop SHALL carry at least one loop_invariant sufficient to explain ... |
| CONV-009 | R0.1 | test | test/unit/conv_review.md:140 | Contract expressions SHALL be pure, per contracts/semantics.md appendix rule 2 (no side effects, no mutation) ... |
| CONV-010 | R0.1 | exception | MX-013 | This library SHALL be buildable and testable with contract-mode = "runtime" (default) and SHALL be ... |
| ARITH-001 | F0 | test, exception | test/unit/arith_test.sv0:72; MX-002 | abs_i32/abs_i64/abs_f64 SHALL be provided with ensures(result >= 0); the signed-integer forms SHALL carry ... |
| ARITH-002 | F0 | test | test/unit/arith_test.sv0:94 | sign_i32/sign_i64/sign_f64 SHALL return -1, 0, or 1 (matching input type) with ensures(result == -1 \ |
| ARITH-003 | F0 | test | test/unit/arith_test.sv0:106 | min_*/max_* SHALL be provided per concrete numeric type with ensures(result == a \ |
| ARITH-004 | F0 | test | test/unit/arith_test.sv0:113 | clamp_*(x, lo, hi) SHALL carry requires(lo <= hi) and ensures(result >= lo && result <= hi) and ensures(lo <= ... |
| ARITH-005 | R0.1 | test | test/unit/arith_test.sv0:121 | floor_f64, ceil_f64, round_f64, trunc_f64 SHALL be provided with ensures pinning each function's rounding ... |
| ARITH-006 | R0.1 | test | test/unit/arith_test.sv0:138 | is_nan_f64, is_finite_f64, is_infinite_f64 SHALL be provided and SHALL be the only sanctioned way this ... |
| ARITH-007 | R0.1 | test | test/unit/arith_test.sv0:151; test/unit/trig_test.sv0:130 | pow_i64(base: i64, exp: u32) -> i64 SHALL use exponentiation by squaring with requires(exp < 64 \ |
| ARITH-008 | R0.1 | test, exception | test/unit/arith_test.sv0:163; MX-002 | add_wrapping_*, sub_wrapping_*, mul_wrapping_* SHALL be provided per integer type with ensures describing ... |
| ARITH-009 | R0.1 | test | test/unit/arith_test.sv0:182 | lerp_f64(a: f64, b: f64, t: f64) -> f64 SHALL carry requires(t >= 0.0 && t <= 1.0) and ensures(t == 0.0 ==> ... |
| ARITH-010 | R0.1 | test | test/unit/arith_test.sv0:187 | fma_f64(a: f64, b: f64, c: f64) -> f64 (fused multiply-add, single rounding) SHALL be provided with ensures ... |
| MOD-001 | R0.1 | test | test/unit/modular_test.sv0:17 | floor_mod_i64(a: i64, m: i64) -> i64 SHALL carry requires(m > 0) and ensures(result >= 0 && result < m) and ... |
| MOD-002 | R0.1 | test | test/unit/modular_test.sv0:25 | gcd_u64(a: u64, b: u64) -> u64 SHALL match contracts/semantics.md's own canonical example: requires(a > 0), ... |
| MOD-003 | R0.1 | test | test/unit/modular_test.sv0:34 | lcm_u64(a: u64, b: u64) -> u64 SHALL carry requires(a > 0 && b > 0), ensures(result % a == 0 && result % b == ... |
| MOD-004 | R0.1 | test | test/unit/modular_test.sv0:43 | add_mod_u64, sub_mod_u64, mul_mod_u64 SHALL each carry requires(m > 0) and ensures(result < m), and SHALL ... |
| MOD-005 | R0.2 | test | test/unit/modular_test.sv0:54 | pow_mod_u64(base: u64, exp: u64, m: u64) -> u64 (modular exponentiation) SHALL carry requires(m > 0), use ... |
| MOD-006 | R0.2 | test | test/property/modular_inverse_property.sv0:14; test/unit/modular_test.sv0:61 | mod_inverse_u64(a: u64, m: u64) -> Option<u64> SHALL be implemented via the extended Euclidean algorithm, ... |
| MOD-007 | R0.2 | test | test/unit/modular_test.sv0:70 | is_congruent_i64(a: i64, b: i64, m: i64) -> bool SHALL carry requires(m > 0) and ensures(result == ... |
| TRIG-001 | R0.2 | test | test/property/trig_identity_property.sv0:21; test/unit/trig_test.sv0:49 | sin_f64, cos_f64 SHALL be total functions over all finite f64 (no requires on domain), implemented with ... |
| TRIG-002 | R0.2 | test | test/unit/trig_test.sv0:76 | tan_f64(x) SHALL be sin_f64(x) / cos_f64(x) with a requires excluding statically-known asymptote inputs is ... |
| TRIG-003 | R0.2 | test | test/unit/trig_test.sv0:83 | asin_f64, acos_f64 SHALL carry requires(x >= -1.0 && x <= 1.0) with companion ... |
| TRIG-004 | R0.2 | test, exception | test/unit/trig_test.sv0:93; MX-007 | atan_f64 SHALL be a total function over all finite f64 with ensures(result > -pi/2 && result < pi/2). ... |
| TRIG-005 | R0.2 | test | test/unit/trig_test.sv0:104 | sinh_f64, cosh_f64, tanh_f64 SHALL be provided via exp_f64 (Section 14, shared with math::arith's pow_f64) ... |
| TRIG-006 | R0.2 | test | test/unit/trig_test.sv0:115 | to_radians_f64(deg: f64) -> f64 and to_degrees_f64(rad: f64) -> f64 SHALL be exact inverses of each other up ... |
| TRIG-007 | R0.2 | test | test/unit/trig_test.sv0:121 | hypot_f64(x: f64, y: f64) -> f64 SHALL compute sqrt(x*x + y*y) using an overflow/underflow-avoiding scaled ... |
| PERF-001 | R0.1 | test | test/unit/trig_test.sv0:8 | docs/accuracy.md SHALL contain one row per non-exact public function with its measured maximum ULP error over ... |
| PERF-002 | R0.2 | test | test/unit/trig_test.sv0:10 | sqrt_f64 SHALL be within **2 ULP** of the reference value over its full non-negative domain fixture; ... |
| PERF-003 | Future | future | — | When a Tier 2 native builtin exists for a function, this library's Tier 1 implementation SHALL be ... |
| PERF-004 | R0.1 | test | test/unit/trig_test.sv0:13 | Integer and modular functions (Section 12) are exact by construction; "accuracy" for them means ... |
| PERF-005 | R0.2 | test | test/unit/trig_test.sv0:17 | Every non-exact function SHALL be benchmarked (iteration/instruction count, not wall-clock, to stay portable ... |
| TEST-001 | F0 | check | scripts/run_unit_tests.py | Every requirement ID in Sections 8, 11, 12, 13, 15, and 17 SHALL map to at least one test in test/. |
| TEST-002 | F0 | check | scripts/check_fixtures.py | Every fixture file SHALL record its provenance: hand-derived, generated from a documented high-precision ... |
| TEST-003 | R0.1 | check | scripts/check_fixtures.py | Domain-boundary and NaN/Infinity/-0.0 cases SHALL be present in every applicable fixture table, not only ... |
| TEST-004 | R0.1 | check | scripts/check_fuzz_budget.py | Property tests SHALL use a fixed, checked-in seed for any pseudo-random sample generation so failures are ... |
| TEST-005 | R0.2 | check, exception | scripts/run_fixture_parity.py; MX-014 | Cross-backend parity tests (16.2) SHALL run in CI for every release-scoped requirement's fixture set. |
| TEST-006 | R1 | check | scripts/ci | The full test suite SHALL pass under ./scripts/sv0 test-guards conventions for .sv0 formatting/lint parity ... |
| POLAR-001 | R0.3 | test | test/unit/polar_test.sv0:18 | struct Polar { pub r: f64, pub theta: f64 } SHALL be defined, Copy (auto-derived, both fields f64). |
| POLAR-002 | R0.3 | test | test/unit/polar_test.sv0:27 | to_polar(x: f64, y: f64) -> Polar SHALL use hypot_f64 (TRIG-007) for r and atan2_f64 (TRIG-004) for theta, ... |
| POLAR-003 | R0.3 | test, exception | test/unit/polar_test.sv0:38; MX-005 | from_polar(p: Polar) -> (f64, f64) (returning (x, y)) SHALL carry requires(p.r >= 0.0) and use ... |
| POLAR-004 | R0.3 | test | test/property/polar_roundtrip_property.sv0:16; test/unit/polar_test.sv0:55 | to_polar composed with from_polar SHALL round-trip within the accuracy budget: ensures-documented as a ... |
| POLAR-005 | R0.3 | test | test/unit/polar_test.sv0:70 | scale_polar(p: Polar, k: f64) -> Polar SHALL carry requires(k >= 0.0) (negative scale is a rotation-by-pi ... |
| CPLX-001 | R0.3 | test | test/unit/complex_test.sv0:28 | struct Complex { pub re: f64, pub im: f64 } SHALL be defined exactly matching sv0doc/type-system/rules.md ... |
| CPLX-002 | R0.3 | test, exception | test/property/complex_algebra_property.sv0:15; test/unit/complex_test.sv0:37; MX-004 | impl Add for Complex, impl Sub for Complex, impl Neg for Complex SHALL implement componentwise arithmetic, ... |
| CPLX-003 | R0.3 | test, exception | test/unit/complex_test.sv0:53; MX-004; MX-006 | impl Mul for Complex SHALL implement (a.re*b.re - a.im*b.im, a.re*b.im + a.im*b.re) and impl Div for Complex ... |
| CPLX-004 | R0.3 | test | test/unit/complex_test.sv0:83 | modulus(c: Complex) -> f64 SHALL be hypot_f64(c.re, c.im) (reusing TRIG-007's overflow-safe form) with ... |
| CPLX-005 | R0.3 | test | test/unit/complex_test.sv0:97 | conjugate(c: Complex) -> Complex SHALL carry ensures(result.re == c.re && result.im == -c.im). ... |
| CPLX-006 | R0.3 | test | test/unit/complex_test.sv0:113 | Complex SHALL NOT auto-derive or hand-implement bitwise Eq as its primary comparison; an approx_eq(a: ... |
| CPLX-007 | Future | test, exception, future | test/unit/complex_test.sv0:126; MX-015 | exp_complex, ln_complex, and pow_complex (Euler's-formula-based complex exponential/logarithm/power) SHALL be ... |
| COMPAT-001 | R0.1 | check | scripts/ci | Every integer and modular function SHALL produce bit-identical results on the C backend (emit-c) and the VM ... |
| COMPAT-002 | R0.2 | check, exception | scripts/run_fixture_parity.py; MX-014 | Every floating-point function SHALL produce results within the same pinned ULP bound (Section 15) on both ... |
| COMPAT-003 | R0.1 | check | .github/sv0-toolchain-pin.txt | This library SHALL compile and pass its full test suite against the audited sv0c/sv0vm revisions in Section ... |
| COMPAT-004 | Future | future | — | If sv0c's generics support matures to the point that a numeric-trait-bounded generic facade (fn abs<T: ... |
| DOC-001 | F0 | check | scripts/check_doc_comments.py | Every public function SHALL have a doc comment stating its domain, its exact contract in prose, and (for ... |
| DOC-002 | R0.1 | process | README.md | README.md SHALL state the Tier 1/Tier 2 distinction (Section 4.3), the current release's scope, and ... |
| DOC-003 | R1 | process | CHANGELOG.md | Each release SHALL record user-visible changes, any tightened or loosened accuracy bound (PERF-002), and any ... |
| DOC-004 | F0 | process | docs/deviations.md | Material changes to a public contract (requires/ensures weakened or strengthened in an incompatible way), an ... |

65 requirements: 10 with check, 11 with exception, 3 with future, 3 with process, 49 with test.
